name: orca_gateway_engine
description: Orca Gateway SDUI Engine — server-driven UI engine for Flutter (Dart backend)
version: 0.1.0
homepage: https://orcagateway.com
repository: https://github.com/orca-gateway/orca-gateway
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  intl: ^0.19.0
  crypto: ^3.0.0
  collection: ^1.18.0

dev_dependencies:
  test: ^1.25.0
  path: ^1.9.0
  lints: ^4.0.0
