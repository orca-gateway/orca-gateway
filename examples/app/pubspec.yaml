name: orcagateway_examples
description: Orca Gateway example app — select and run any demo.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.3

dependencies:
  orca_google_map:
    path: ../../plugins/orca_google_map
  flutter:
    sdk: flutter
  flutter_confetti: ^0.5.1
  orca_gateway:
    path: ../../sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
