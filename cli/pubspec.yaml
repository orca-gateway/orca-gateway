name: orca_gateway_cli
description: "CLI tool for Orca Gateway — manage plugins, native config, and scaffolding."
version: 0.1.0

environment:
  sdk: ^3.11.3

executables:
  orca: orca

dependencies:
  args: ^2.6.0
  yaml: ^3.1.0
  xml: ^6.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
