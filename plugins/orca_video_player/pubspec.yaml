name: orca_video_player
description: "Orca Gateway plugin for video playback — server-driven video player with play/pause/seek controls and progress events."
version: 0.1.1
homepage: https://github.com/amrebada/orca-gateway
repository: https://github.com/amrebada/orca-gateway

environment:
  sdk: ^3.11.3
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  orca_gateway: ^0.2.0
  video_player: ^2.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
