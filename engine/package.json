{
  "name": "@orca-gateway/engine",
  "version": "0.2.2",
  "description": "Orca Gateway SDUI Engine — server-driven UI for Flutter",
  "type": "module",
  "author": "Amr Ebada <hello@orcagateway.com>",
  "license": "BUSL-1.1",
  "homepage": "https://orcagateway.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/orca-gateway/orca-gateway.git",
    "directory": "open-source/engine"
  },
  "bugs": {
    "url": "https://github.com/orca-gateway/orca-gateway/issues"
  },
  "keywords": [
    "sdui",
    "server-driven-ui",
    "flutter",
    "bun",
    "typescript",
    "orca-gateway",
    "orcagateway"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./core": {
      "types": "./dist/core/index.d.ts",
      "import": "./dist/core/index.js"
    },
    "./types": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/types/index.js"
    },
    "./components": {
      "types": "./dist/components/index.d.ts",
      "import": "./dist/components/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "bun": ">=1.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "dev:examples": "bun run --watch ../examples/server/index.ts",
    "clean": "rm -rf dist",
    "build:types": "tsc -p tsconfig.build.json",
    "build:js": "bun build src/index.ts --outdir dist --minify --target bun --format esm && bun build src/core/index.ts --outdir dist/core --minify --target bun --format esm && bun build src/types/index.ts --outdir dist/types --minify --target bun --format esm && bun build src/components/index.ts --outdir dist/components --minify --target bun --format esm",
    "build": "bun run clean && bun run build:types && bun run build:js",
    "prepublishOnly": "bun run build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
