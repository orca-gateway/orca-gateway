export type ImageFit = "fill" | "contain" | "cover" | "fitWidth" | "fitHeight" | "none" | "scaleDown";

// How an image should be tiled when the target area is larger than the image.
// Mirrors Flutter's ImageRepeat enum.
export type ImageRepeat = "repeat" | "repeatX" | "repeatY" | "noRepeat";
